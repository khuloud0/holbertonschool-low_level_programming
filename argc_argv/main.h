#ifndef MAIN_H
#define MAIN_H

/* Recursion tasks */
int _putchar(char c);
void _puts_recursion(char *s);
void _print_rev_recursion(char *s);
int _strlen_recursion(char *s);
int factorial(int n);
int _pow_recursion(int x, int y);
int _sqrt_recursion(int n);
int is_prime_number(int n);

/* Argc & argv tasks */
int main(int argc, char *argv[]); /* Optional declaration if needed */

/* Add more prototypes as needed for future tasks */

#endif /* MAIN_H */
