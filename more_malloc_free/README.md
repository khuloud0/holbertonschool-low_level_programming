# malloc_free - Holberton School

This directory contains exercises on dynamic memory allocation using `malloc` and `free`.

## Compilation

```bash
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 <file>.c -o <output>
